---
import LinkButton from "../atoms/LinkButton.astro";
import BenefitGrid, { type Benefit } from "../molecules/BenefitGrid.astro";

const benefits: Benefit[] = [
	{
		icon: "🧭",
		title: "Clientes llegan directo a tu local",
		description: "Geolocalización y notificaciones push en el momento exacto",
		stat: "50% de ventas provienen de ofertas cerca de mí",
	},
	{
		icon: "💬",
		title: "Chat directo cliente-negocio",
		description: "Convierte dudas en pedidos al instante",
		stat: "Meta ≥ 15% de conversión",
	},
	{
		icon: "🎯",
		title: "Comisiones bajas (3%)",
		description: "Plan Pueblia Plus con tarifas competitivas",
		stat: "Ahorro vs. OTAs de hasta 12%",
	},
	{
		icon: "📊",
		title: "Dashboard sencillo",
		description: "Analíticas claras y % de perfil completado",
		stat: "Más visibilidad = perfil completo",
	},
];
---

<section id="business">
	<div class="container">
		<h2 class="section-title">Para tu negocio en Guatapé</h2>
		<p class="section-subtitle">
			Atrae más clientes locales y turistas con herramientas que realmente
			funcionan
		</p>

		<BenefitGrid benefits={benefits} />

		<div class="cta-center">
			<LinkButton href="/register" variant="secondary"
				>📊 Listar mi negocio</LinkButton
			>
		</div>
	</div>
</section>

<style>
	#business {
		padding: var(--spacing-xxxxl) 0;
		background: linear-gradient(
			135deg,
			var(--secondary) 20%,
			var(--top-color) 100%
		);
		position: relative;
		overflow: hidden;
	}

	.container {
		max-width: 1500px;
		margin: 0 auto;
		padding: 0 var(--spacing-lg);
		position: relative;
		z-index: 1;
	}

	.section-title {
		font-size: clamp(2rem, 4vw, 3.5rem);
		font-weight: 800;
		text-align: center;
		color: var(--text-color);
		margin-bottom: var(--spacing-lg);
		position: relative;
	}

	.section-title::after {
		content: "";
		position: absolute;
		bottom: -var(--spacing-sm);
		left: 50%;
		transform: translateX(-50%);
		width: 80px;
		height: 4px;
		background: linear-gradient(90deg, var(--primary), var(--secondary));
		border-radius: 2px;
	}

	.section-subtitle {
		font-size: clamp(1.1rem, 2.5vw, 1.4rem);
		text-align: center;
		color: var(--text-muted);
		margin-bottom: var(--spacing-xxl);
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
		line-height: 1.6;
	}

	.cta-center {
		display: flex;
		align-items: center;
		justify-content: center;
	}
</style>
