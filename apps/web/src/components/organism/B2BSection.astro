<section id="business">
	<div class="container">
		<h2 class="section-title">Para tu negocio en Guatapé</h2>
		<p class="section-subtitle">
			Atrae más clientes locales y turistas con herramientas que realmente
			funcionan
		</p>

		<div class="benefits-grid">
			<div class="benefit-card">
				<div class="benefit-icon">🧭</div>
				<h3>Clientes llegan directo a tu local</h3>
				<p>Geolocalización y notificaciones push en el momento exacto</p>
				<span class="benefit-stat shine"
					>50% de ventas provienen de ofertas "cerca de mí"</span
				>
			</div>

			<div class="benefit-card">
				<div class="benefit-icon">💬</div>
				<h3>Chat directo cliente-negocio</h3>
				<p>Convierte dudas en pedidos al instante</p>
				<span class="benefit-stat shine">Meta ≥ 15% de conversión</span>
			</div>

			<div class="benefit-card">
				<div class="benefit-icon">🎯</div>
				<h3>Comisiones bajas (3%)</h3>
				<p>Plan "Pueblia Plus" con tarifas competitivas</p>
				<span class="benefit-stat shine">Ahorro vs. OTAs de hasta 12%</span>
			</div>

			<div class="benefit-card">
				<div class="benefit-icon">📊</div>
				<h3>Dashboard sencillo</h3>
				<p>Analíticas claras y % de perfil completado</p>
				<span class="benefit-stat shine">Más visibilidad = perfil completo</span
				>
			</div>
		</div>

		<div class="cta-center">
			<button class="btn btn-accent shine">📊 Listar mi negocio</button>
		</div>
	</div>
</section>

<style>
	#business {
		padding: var(--spacing-xxxxl) 0;
		background: linear-gradient(
			135deg,
			var(--secondary) 20%,
			var(--top-color) 100%
		);
		position: relative;
		overflow: hidden;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 var(--spacing-lg);
		position: relative;
		z-index: 1;
	}

	.section-title {
		font-size: clamp(2rem, 4vw, 3.5rem);
		font-weight: 800;
		text-align: center;
		color: var(--text-color);
		margin-bottom: var(--spacing-lg);
		position: relative;
	}

	.section-title::after {
		content: "";
		position: absolute;
		bottom: -var(--spacing-sm);
		left: 50%;
		transform: translateX(-50%);
		width: 80px;
		height: 4px;
		background: linear-gradient(90deg, var(--primary), var(--secondary));
		border-radius: 2px;
	}

	.section-subtitle {
		font-size: clamp(1.1rem, 2.5vw, 1.4rem);
		text-align: center;
		color: var(--text-muted);
		margin-bottom: var(--spacing-xxl);
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
		line-height: 1.6;
	}

	.benefits-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: var(--spacing-xl);
		margin-bottom: var(--spacing-xxl);
	}

	.benefit-card {
		background: var(--top-color);
		padding: var(--spacing-xl);
		border-radius: var(--border-radius);
		border: 1px solid var(--border-color);
		box-shadow: 0 8px 25px var(--shadow-color);
		transition: all var(--transition-duration) ease;
		position: relative;
		overflow: hidden;
	}

	.benefit-card::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 4px;
		background: linear-gradient(90deg, var(--primary), var(--secondary));
		transform: scaleX(0);
		transition: transform var(--transition-duration) ease;
	}

	.benefit-card:hover {
		transform: translateY(-8px);
		box-shadow: 0 15px 40px var(--shadow-color);
	}

	.benefit-card:hover::before {
		transform: scaleX(1);
	}

	.benefit-icon {
		font-size: 3rem;
		margin-bottom: var(--spacing-md);
		display: block;
		text-align: center;
		filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
	}

	.benefit-card h3 {
		font-size: 1.4rem;
		font-weight: 700;
		color: var(--text-color);
		margin-bottom: var(--spacing-md);
		text-align: center;
		line-height: 1.3;
	}

	.benefit-card p {
		color: var(--text-muted);
		margin-bottom: var(--spacing-lg);
		text-align: center;
		line-height: 1.5;
	}

	.benefit-stat {
		display: block;
		font-size: 0.9rem;
		font-weight: 600;
		background: var(--secondary-light);
		padding: var(--spacing-sm) var(--spacing-md);
		border-radius: calc(var(--border-radius) / 2);
		text-align: center;
		border: 1px solid var(--secondary);
	}

	.cta-center {
		text-align: center;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		gap: var(--spacing-sm);
		font-size: 1.1rem;
		font-weight: 600;
		padding: var(--spacing-lg) var(--spacing-xl);
		border: none;
		border-radius: var(--border-radius);
		cursor: pointer;
		transition: all var(--transition-duration) ease;
		text-decoration: none;
		position: relative;
		overflow: hidden;
	}

	.btn-accent {
		background-color: var(--secondary);
		color: var(--top-color);
		box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
	}

	.btn-accent:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
	}

	.btn-accent:active {
		transform: translateY(0);
	}
</style>
